%% MyMainScript

load('../input/Q1data.mat');

%loop
C = corresp(A,B);
[R,T] = rigidtrans(A,C);


while norm(R-eye(3),'fro')> 0.000001 | norm(T,'fro') > 0.000001 
    B = B*R'+ones(size(B))*diag(T);
    C = corresp(A,B);
    [R,T] = rigidtrans(A,C);

end
